# KYDW 团队学术网站改进建议（分析版）

> 目标：把当前“静态展示页”升级为“可持续运营的团队学术门户”，在不增加维护负担的前提下，提升内容管理效率、展示效果与多人协作友好性。

## 1. 现状诊断（基于仓库结构）

### 1.1 内容管理方式不统一
- 当前首页内容主要写在 `index.html` 中，人员/活动/成果是手写 HTML 卡片和段落。
- 同时仓库里又有 `contents/*.md` + `contents/config.yml` + `static/js/scripts.js` 这套“YAML/Markdown 动态注入”机制，但与 `index.html` 并未统一。

**影响**
- 维护入口分散：不同人不知道该改 `index.html` 还是 `contents/`。
- 无法让不熟悉前端的同学稳定编辑（容易改坏标签结构）。

### 1.2 数据结构缺失（成员/论文/项目不可结构化维护）
- 成员、活动、成果目前都直接写在 HTML 里，缺少统一字段（如姓名、角色、年份、链接、标签、封面图等）。

**影响**
- 不支持筛选、排序、按年份归档。
- 增删改需要改代码，无法做到“填表式更新”。

### 1.3 展示层存在明显可优化点
- 导航与页面内容是全英文，但团队受众很可能中英混合；叙事密度较高、扫描效率偏低。
- 若干活动链接仍为 `example.com` 占位。
- 视觉上目前以 Bootstrap 默认风格为主，品牌识别度与“学术质感”可进一步提升。

### 1.4 协作与质量保障不足
- 仓库暂无“内容编辑规范”“字段说明”“上线前检查清单”。
- 缺少基础 CI 校验（例如 JSON/YAML 格式检查、链接检查、构建检查）。

---

## 2. 改造原则

1. **内容优先**：编辑体验先于炫技动画。
2. **低门槛协作**：让不会前端的人也能安全修改。
3. **结构化数据驱动页面**：成员/论文/项目都由数据文件生成。
4. **渐进式演进**：先“可维护”，再“更高级”。

---

## 3. 建议方案（从低风险到高收益）

## A. 先做“轻量重构”（1~2 周，可快速落地）

### A1. 统一内容入口
- 选定一种来源：
  - 方案 A：保留纯静态，但把主内容迁移为 `data/*.json`，页面仅负责渲染。
  - 方案 B：完整使用现有 `contents/*.md` + `config.yml` 机制，并扩展 section。
- 建议优先 **方案 A（JSON 数据驱动）**，因为结构化更适合人员/论文/项目管理。

### A2. 抽离数据文件（关键）
建议新增：
- `data/members.json`
- `data/publications.json`
- `data/projects.json`
- `data/news.json`

示例字段（成员）：
- `name`, `name_en`, `role`, `group`, `start_year`, `end_year`, `affiliation`, `interests`, `avatar`, `homepage`, `email_visible`

示例字段（论文）：
- `title`, `authors`, `venue`, `year`, `type`, `tags`, `doi`, `pdf`, `code`, `highlight`

### A3. 增加“编辑者友好文档”
- 新建 `docs/content-guide.md`：
  - 每个数据文件字段解释
  - 一条记录如何新增
  - 常见错误与排查
- 新建 `docs/release-checklist.md`：
  - 链接可用性
  - 图片路径
  - 时间排序
  - 中英文格式一致性

### A4. 前端展示优化（不大改框架）
- 首页首屏增加明确 CTA（Join / Publications / Contact）。
- 成员页：按“在任/往届/导师”分组+搜索。
- 论文页：按年份折叠 + 标签筛选 + 一键复制引用（BibTeX 可后续加）。
- 项目页：卡片化展示（状态、负责人、链接、摘要）。

---

## B. 升级“可视化维护能力”（2~4 周）

### B1. 接入 Git-based CMS（强烈推荐）
推荐：**Decap CMS（原 Netlify CMS）**。
- 在仓库内配置 `admin/config.yml`。
- 编辑者通过网页表单维护 JSON/Markdown，提交即自动生成 PR。

**好处**
- 非技术同学可以像填后台一样更新内容。
- 仍保留 Git 审核和版本追踪。

### B2. 标准化模板
- 论文条目模板（必填字段校验）
- 新闻模板（日期、封面、摘要）
- 项目模板（目标、进展、成果）

### B3. 自动校验（CI）
GitHub Actions：
- JSON/YAML lint
- dead link check
- 简单构建检查
- 可选：图片尺寸/体积阈值检查

---

## C. 中长期升级（可选）

### C1. 迁移到静态站点生成器
优先级建议：**Astro / Hugo / Docusaurus**（择一）。
- 统一“数据 + 模板 + 构建”，降低 HTML 手工维护成本。
- 可自动生成成员页、论文详情页、项目详情页。

### C2. 国际化与传播优化
- 中英双语切换（至少首页和成果页）。
- SEO 基础增强：OpenGraph、结构化元数据、sitemap。
- 为每条论文/项目生成可分享链接卡片。

---

## 4. 信息架构建议（建议改版后的导航）

- Home（团队定位 + 最新成果）
- People（成员）
- Publications（论文）
- Projects（项目）
- News（动态）
- Join（招新/合作）
- Resources（可选：课程、资料、工具）
- Contact

---

## 5. 面向你们团队的“现实可执行路线”

### 第 0 周（立即）
- 锁定唯一内容源（建议数据文件）。
- 清理占位链接与过期信息。

### 第 1 周
- 完成员/论文/项目 JSON 化。
- 前端实现按数据渲染列表。

### 第 2 周
- 完成筛选、排序、搜索。
- 补充编辑文档 + 发布清单。

### 第 3~4 周
- 接入 Decap CMS + GitHub Actions 校验。
- 组织一次“非技术同学试用编辑”并修正文档。

---

## 6. 视觉风格建议（简洁版）

- 色彩：1 个主色 + 1 个强调色 + 大面积留白。
- 字体：中文与英文字体分层（标题/正文/数字）。
- 卡片：统一圆角、阴影、边距体系。
- 图片：成员头像统一比例（1:1），项目封面统一比例（16:9）。
- 动效：仅保留轻量 hover/滚动渐显，避免花哨干扰学术信息。

---

## 7. 快速优先级清单（建议先做）

P0（必须）
1. 抽离结构化数据（成员/论文/项目）
2. 清理链接与内容真实性
3. 建立编辑指南

P1（高价值）
1. 筛选/搜索/按年份归档
2. CMS 表单化编辑
3. 自动化校验

P2（锦上添花）
1. 双语
2. SEO/分享卡片
3. 视觉品牌升级

---

如果你愿意，我下一步可以直接给你一份“**最小可用改造（MVP）实施清单**”：包含文件树、数据 schema、前端渲染函数拆分方式，以及一版可直接复制的 `members.json / publications.json` 模板。
